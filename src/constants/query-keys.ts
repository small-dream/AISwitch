/** TanStack Query 缓存键（唯一事实来源） */
export const QUERY_KEYS = {
  presets: ['presets'] as const,
  toolStatus: ['tool-status'] as const,
  baseline: ['baseline'] as const,
  restorePlan: ['restore-plan'] as const,
}
