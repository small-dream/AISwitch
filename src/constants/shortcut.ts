/** 全局快捷键（US-20）：集中管理，便于后续扩展自定义绑定设置页 */
export const SHORTCUTS = {
  /** 呼出并聚焦主窗口（应用隐藏至托盘后仍可唤起） */
  showWindow: 'CmdOrCtrl+Shift+A',
  /** 循环切换当前工具的下一个预设 */
  nextPreset: 'CmdOrCtrl+Shift+S',
} as const
